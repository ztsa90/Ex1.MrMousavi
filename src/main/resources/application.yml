server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tb1_Mousavi
    username: postgres
    password: sanaamiri

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true